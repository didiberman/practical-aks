apiVersion: v1
kind: Service
metadata:
  name: aks-learning-app
  namespace: default
  labels:
    app: aks-learning-app
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: aks-learning-app
  type: ClusterIP
